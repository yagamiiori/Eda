(function(){
  var __zz = function() {
    var __o = jp.atlantis.mashiron.ads['ads1'];
    if (typeof __o == 'undefined') {
      setTimeout(__zz, 99);
    } else {
      try {
        __o.setVars({
    "type": "externalNetwork",
    "width": 120,
    "height": 240,
    "iframeLocation": "http://ad.adlantis.jp/ad/load_iframe?key=MTU5NzdfMF8zNTQ0XzU%253D%250A"});
      } catch(e) {
        setTimeout(__zz, 99);
      }
      return true;
    }
  };
  __zz.apply();
})();
