
・var：変数の型を推論してくれる。var x = 1; なら x は int になる。
・必ず初期値を伴う必要がある。
　初期値から変数の型を自動判別（型推論）するものが型推論であるため
　初期値がなければ推論のしようがない。
　※ var x;　とかは初期値がないので無理

・MyTestClass test = new MyTestClass();
　↑こういう冗長さを省くため左辺側の型名を省略できる機能だと思ってください。
　　「var  test = new MyTestClass();」として冗長さを省けるってこと

